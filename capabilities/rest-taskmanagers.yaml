naftiko: 1.0.0-alpha2
info:
  label: Apache Flink REST API — TaskManagers
  description: 'Apache Flink REST API — TaskManagers. 3 operations. Lead operation: List TaskManagers. Self-contained Naftiko
    capability covering one Flink business surface.'
  tags:
  - Flink
  - TaskManagers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FLINK_API_KEY: FLINK_API_KEY
capability:
  consumes:
  - type: http
    namespace: rest-taskmanagers
    baseUri: http://localhost:8081
    description: Apache Flink REST API — TaskManagers business capability. Self-contained, no shared references.
    resources:
    - name: taskmanagers
      path: /taskmanagers
      operations:
      - name: listtaskmanagers
        method: GET
        description: List TaskManagers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: taskmanagers-taskmanagerid-logs
      path: /taskmanagers/{taskmanagerid}/logs
      operations:
      - name: listtaskmanagerlogs
        method: GET
        description: List TaskManager logs
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: taskmanagerid
          in: path
          type: string
          required: true
    - name: taskmanagers-taskmanagerid-metrics
      path: /taskmanagers/{taskmanagerid}/metrics
      operations:
      - name: gettaskmanagermetrics
        method: GET
        description: TaskManager metrics
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: taskmanagerid
          in: path
          type: string
          required: true
  exposes:
  - type: rest
    namespace: rest-taskmanagers-rest
    port: 8080
    description: REST adapter for Apache Flink REST API — TaskManagers. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/taskmanagers
      name: taskmanagers
      description: REST surface for taskmanagers.
      operations:
      - method: GET
        name: listtaskmanagers
        description: List TaskManagers
        call: rest-taskmanagers.listtaskmanagers
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/taskmanagers/{taskmanagerid}/logs
      name: taskmanagers-taskmanagerid-logs
      description: REST surface for taskmanagers-taskmanagerid-logs.
      operations:
      - method: GET
        name: listtaskmanagerlogs
        description: List TaskManager logs
        call: rest-taskmanagers.listtaskmanagerlogs
        with:
          taskmanagerid: rest.taskmanagerid
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/taskmanagers/{taskmanagerid}/metrics
      name: taskmanagers-taskmanagerid-metrics
      description: REST surface for taskmanagers-taskmanagerid-metrics.
      operations:
      - method: GET
        name: gettaskmanagermetrics
        description: TaskManager metrics
        call: rest-taskmanagers.gettaskmanagermetrics
        with:
          taskmanagerid: rest.taskmanagerid
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rest-taskmanagers-mcp
    port: 9090
    transport: http
    description: MCP adapter for Apache Flink REST API — TaskManagers. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: list-taskmanagers
      description: List TaskManagers
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-taskmanagers.listtaskmanagers
      outputParameters:
      - type: object
        mapping: $.
    - name: list-taskmanager-logs
      description: List TaskManager logs
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-taskmanagers.listtaskmanagerlogs
      with:
        taskmanagerid: tools.taskmanagerid
      outputParameters:
      - type: object
        mapping: $.
    - name: taskmanager-metrics
      description: TaskManager metrics
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-taskmanagers.gettaskmanagermetrics
      with:
        taskmanagerid: tools.taskmanagerid
      outputParameters:
      - type: object
        mapping: $.
